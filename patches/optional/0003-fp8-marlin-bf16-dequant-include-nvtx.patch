diff --git a/vllm/envs.py b/vllm/envs.py
index 67f91f7e1..9939b7510 100755
--- a/vllm/envs.py
+++ b/vllm/envs.py
@@ -197,6 +197,7 @@ if TYPE_CHECKING:
     VLLM_SPARSE_DENSE_QUERY_BLOCK_DECODE: int = -1
     VLLM_MARLIN_FP8_DEQUANT_BF16: bool = False
     VLLM_MARLIN_FP8_DEQUANT_EXCLUDE: str = ""
+    VLLM_MARLIN_FP8_DEQUANT_INCLUDE: str = ""
     VLLM_DSPARK_FUSED_MARKOV: bool = True
     VLLM_LOCAL_ARGMAX_ALLREDUCE: bool = False
     VLLM_MARLIN_INPUT_DTYPE: Literal["int8", "fp8"] | None = None
@@ -1708,6 +1709,12 @@ environment_variables: dict[str, Callable[[], Any]] = {
     "VLLM_MARLIN_FP8_DEQUANT_EXCLUDE": lambda: os.environ.get(
         "VLLM_MARLIN_FP8_DEQUANT_EXCLUDE", ""
     ),
+    # Optional positive filter: when set, only matching layers may dequant
+    # to BF16. This targets specific FP8 dense paths without changing the
+    # default global behavior.
+    "VLLM_MARLIN_FP8_DEQUANT_INCLUDE": lambda: os.environ.get(
+        "VLLM_MARLIN_FP8_DEQUANT_INCLUDE", ""
+    ),
     # Fuse each DSpark Markov draft step (embedding lookup, transition GEMV,
     # base-logit add, padding mask, shard argmax) into one Triton pass, so the
     # [batch, shard_width] logit row is reduced where it is produced instead of
diff --git a/vllm/model_executor/kernels/linear/scaled_mm/marlin.py b/vllm/model_executor/kernels/linear/scaled_mm/marlin.py
index 3198ccad1..bf1bed9b0 100644
--- a/vllm/model_executor/kernels/linear/scaled_mm/marlin.py
+++ b/vllm/model_executor/kernels/linear/scaled_mm/marlin.py
@@ -32,10 +32,18 @@ logger = init_logger(__name__)
 
 
 def _dequant_excluded(layer: torch.nn.Module) -> bool:
+    prefix = getattr(layer, "prefix", "")
+    include_patterns = [
+        p for p in envs.VLLM_MARLIN_FP8_DEQUANT_INCLUDE.split(",") if p
+    ]
+    if include_patterns and not is_layer_skipped(
+        prefix, include_patterns, skip_with_substr=True
+    ):
+        logger.debug("keeping %s on Marlin (dequant include miss)", prefix)
+        return True
     patterns = [p for p in envs.VLLM_MARLIN_FP8_DEQUANT_EXCLUDE.split(",") if p]
     if not patterns:
         return False
-    prefix = getattr(layer, "prefix", "")
     excluded = is_layer_skipped(prefix, patterns, skip_with_substr=True)
     if excluded:
         logger.debug("keeping %s on Marlin (dequant exclusion)", prefix)
@@ -136,9 +144,10 @@ class MarlinFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):
         replace_parameter(layer, "weight", weight_dq)
         del layer.weight_scale_inv
         layer.marlin_fp8_dequant = True
-        logger.debug(
-            "fp8->%s dequant for cuBLAS: (%d, %d) freed %d B, allocated %d B",
+        logger.info(
+            "fp8->%s dequant for cuBLAS: prefix=%s shape=(%d, %d) freed %d B, allocated %d B",
             layer.orig_dtype,
+            getattr(layer, "prefix", ""),
             n,
             k,
             freed,
@@ -152,11 +161,38 @@ class MarlinFP8ScaledMMLinearKernel(FP8ScaledMMLinearKernel):
         bias: torch.Tensor | None = None,
     ) -> torch.Tensor:
         if getattr(layer, "marlin_fp8_dequant", False):
+            if envs.VLLM_NVTX_SCOPES_FOR_PROFILING:
+                torch.cuda.nvtx.range_push(
+                    f"linear:marlin_fp8_dequant_bf16:{getattr(layer, 'prefix', '')}:"
+                    f"x={tuple(x.shape)}:w={tuple(layer.weight.shape)}"
+                )
+                try:
+                    return torch.nn.functional.linear(x, layer.weight, bias)
+                finally:
+                    torch.cuda.nvtx.range_pop()
             return torch.nn.functional.linear(x, layer.weight, bias)
         if self.block_quant:
             weight_scale = layer.weight_scale_inv
         else:
             weight_scale = layer.weight_scale
+        if envs.VLLM_NVTX_SCOPES_FOR_PROFILING:
+            torch.cuda.nvtx.range_push(
+                f"linear:marlin_fp8:{getattr(layer, 'prefix', '')}:"
+                f"x={tuple(x.shape)}:w={tuple(layer.weight.shape)}"
+            )
+            try:
+                return apply_fp8_marlin_linear(
+                    input=x,
+                    weight=layer.weight,
+                    weight_scale=weight_scale,
+                    workspace=layer.workspace,
+                    size_n=layer.output_size_per_partition,
+                    size_k=layer.input_size_per_partition,
+                    input_dtype=self.marlin_input_dtype,
+                    bias=bias,
+                )
+            finally:
+                torch.cuda.nvtx.range_pop()
         return apply_fp8_marlin_linear(
             input=x,
             weight=layer.weight,
diff --git a/vllm/model_executor/layers/quantization/fp8.py b/vllm/model_executor/layers/quantization/fp8.py
index 49dd3a6d2..782ee575a 100644
--- a/vllm/model_executor/layers/quantization/fp8.py
+++ b/vllm/model_executor/layers/quantization/fp8.py
@@ -474,6 +474,15 @@ class Fp8LinearMethod(LinearMethodBase):
                     else:
                         # Fallback
                         weight_bf16 = weight_fp8 * weight_scale
+                if envs.VLLM_NVTX_SCOPES_FOR_PROFILING:
+                    torch.cuda.nvtx.range_push(
+                        f"linear:fp8_batch_invariant_bf16:{getattr(layer, 'prefix', '')}:"
+                        f"x={tuple(x.shape)}:w={tuple(weight_bf16.shape)}"
+                    )
+                    try:
+                        return torch.nn.functional.linear(x, weight_bf16.t(), bias)
+                    finally:
+                        torch.cuda.nvtx.range_pop()
                 return torch.nn.functional.linear(x, weight_bf16.t(), bias)
 
         return self.fp8_linear.apply_weights(layer, x, bias)
